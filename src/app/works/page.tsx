import { Metadata } from "next";

export const metadata: Metadata = {
  title: "James Currie - Works",
  description: "Motion Graphics Designer.",
};

export default function Works() {
  return <div>Works</div>;
}
